// The geometry of any structure can be represented by these two simple entities.

export type Node = [number, number, number]; // Represents the position coordinates [x,y,z]
export type Element = [number, number]; // Represents the indices of the first and second node in the list of nodes
