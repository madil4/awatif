import { createSignal } from "solid-js";

export const [nodes, setNodes] = createSignal([]);
export const [elements, setElements] = createSignal([]);
export const [text, setText] = createSignal("");
export const [supports, setSupports] = createSignal([]);
