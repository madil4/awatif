{
  "name": "website",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "rm -rf dist && concurrently --kill-others \"tailwindcss -i ./tailwind.css -o ./src/css/style.css --watch\" \"eleventy --serve\"",
    "build": "rm -rf dist && eleventy"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "~2.0.1",
    "@11ty/eleventy-server-browsersync": "~1.0.2",
    "concurrently": "~8.2.2",
    "preline": "~2.1.0",
    "tailwindcss": "~3.4.3"
  }
}
