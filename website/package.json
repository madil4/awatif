{
  "name": "website",
  "description": "Awatif Website https://awatif.co",
  "scripts": {
    "dev": "rimraf dist && concurrently --kill-others \"tailwindcss -i ./tailwind.css -o ./src/css/style.css --watch\" \"eleventy --serve\"",
    "build": "rimraf dist && eleventy"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "~3.0.0",
    "@11ty/eleventy-server-browsersync": "~1.0.2",
    "concurrently": "~9.0.1",
    "preline": "~2.5.1",
    "rimraf": "^6.0.1",
    "tailwindcss": "~3.4.14"
  }
}
